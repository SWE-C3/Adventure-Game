jobs:
  build:
    docker:
      - image: python:3.7.1
    steps:
      - run: python -m pip install virtualenv
      - run: python -m virtualenv venv
      - run: . venv/bin/activate
      - run: pip install -r requirements.txt
      - run: autopep8 . --recursive --in-place --exclude venv